{
  "name": "rebel-messenger",
  "version": "1.0.0",
  "description": "Rebel Messenger - simple WebRTC chat + audio/video + file sharing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "selftest:headless": "node scripts/selftest-headless.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  }
}
